kubernetes-app:
  prefix: ""
  namespace: pipeline

kafka-connector:
  prefix: ""
  namespace: pipeline

kafka-app:
  app:
    streams:
      brokers: ${broker}
      schemaRegistryUrl: ${schema_registry_url}

producer:
  app:
    labels:
      pipeline: ${pipeline_name}

streams-app:
  app:
    labels:
      pipeline: ${pipeline_name}
