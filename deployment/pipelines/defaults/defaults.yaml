kubernetes-app:
  prefix: ""
  namespace: pipeline

kafka-connector:
  prefix: ""
  namespace: pipeline

kafka-app:
  app:
    labels:
      pipeline: ${pipeline_name}
    streams:
      brokers: ${broker}
      schemaRegistryUrl: ${schema_registry_url}
    prometheus:
      jmx:
        enabled: false
